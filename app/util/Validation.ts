/* export const ValidateRegisterFields = (firstName: string, lastName: string, email: string, interestField: number, password: string) : string => {
    if (firstName && firstName.length < 0) {
        return 'Please all details'
    }
} */